# 물때 (multtae)

한국 가정용 식물·분재 물주기 앱. 무료, 계정 없음, 사진 서버 미저장.
전체 기획은 SPEC.md. 태스크 번호(예: 2-3)는 SPEC.md 16장 표를 가리킨다.

## 스택

- apps/mobile: Expo SDK 최신 안정, TypeScript strict, expo-router, zustand, drizzle-orm + expo-sqlite
- supabase/functions: Deno Edge Functions (identify, light-grade, species, diagnose, weather). 순수 로직은 _shared 에 두고 vitest 로 테스트하며, Deno 전용 API 는 각 함수의 index.ts 에만 둔다. 배포는 pnpm functions:deploy (npx supabase, 로그인 필요)
- 테스트: vitest. src/engine 은 커버리지 100% 유지
- 패키지 매니저: pnpm, 워크스페이스

## 절대 규칙

- 사용자 사진·식물·공간 데이터를 서버에 저장하는 코드를 쓰지 않는다
- API 키는 앱 번들에 넣지 않는다. Edge Function 환경변수만 사용
- 물주기 엔진(src/engine)은 UI·DB에 의존하지 않는 순수 함수. 계수는 인자로 받는다
- 색은 src/ui/tokens.ts 의 토큰만 사용. 상태는 색만으로 구분하지 않고 이름표 글자나 체크 표시를 함께 쓴다 (디자인은 SPEC 14장 화원 라벨)
- 앱 내 문구는 존댓말, 느낌표 없음, 이모지 없음, 대시 문장부호 없음
- 하드코딩 한국어 문자열 금지, src/i18n/ko.ts 키 사용
- 명세와 다르게 해야 하는 이유가 생기면 구현 전에 먼저 묻는다

## 엔진 요약 (SPEC.md 5장)

I = clamp(B × S[g][season] × P × L × T × M × U, 1, 60)
계절 경계: 봄 3/1, 장마 6/21, 폭염 7/26, 가을 9/1, 겨울 11/16 (Asia/Seoul)
학습 U: wet ×1.15, dry ×0.85, droop 추가 ×0.9, 범위 0.5~2.0
테스트 케이스: SPEC.md 5.6 표 6개

## 작업 방식

- 세션당 태스크 1개. 시작 시 이 파일과 SPEC.md 해당 섹션을 읽는다
- 엔진·계절·스케줄러는 테스트 먼저 작성
- 끝나면 아래 진행 표를 갱신하고 커밋 메시지에 태스크 번호를 넣는다
- 화면에는 사용자가 원하는 결과만 보여 준다. 계산식·계수·내부 용어(계산, 기본값, 등급, 보정)는 문구에 쓰지 않고, 어색하거나 기계적인 문장은 쓰지 않는다

## 진행 상황

| 태스크 | 상태 | 메모 |
| --- | --- | --- |
| 1-1 | 완료 | 2026-09-20. Expo SDK 57(RN 0.86)·pnpm 12·drizzle·vitest. 화면은 apps/mobile/app, 별칭 @/ 는 src/. 시뮬레이터는 Expo Go로 확인. 5.6 테스트 6개는 1-2 구현 전까지 실패가 정상 |
| 1-2 | 완료 | 2026-09-20. computeInterval(mode: computed·hydro·manual, belowMin)·applyFeedback·applyRepot, 테스트 50개·커버리지 100%. 우선순위는 수동 고정 > 수경 > 계산, 고정 주기는 60일 상한 미적용. 반올림은 float 오차 보정(1e-9) 포함. 미루기·마지막 물 준 날 모름·다음 날짜 계산은 날짜 로직이라 1-3 이후로 남김 |
| 1-3 | 완료 | 2026-09-20. season.ts(getSeason·getSeasonAt·nextSeasonChange), calendar.ts(toSeoulDate·toCalendarDate·startOfDay·addDays·diffDays), nextWaterDate(미룬 횟수 포함)·halfIntervalDays. 계절은 Asia/Seoul 날짜로 판정, 날짜 연산은 UTC 오프셋을 인자로 받아 실행 환경 시간대와 무관(6개 TZ에서 확인). 테스트 108개·커버리지 100%. 미루기 최대 3회 규칙, "모름" 등록의 저장 방식, 계절 경계 조정값 저장은 2-2·2-3·설정에서 결정 |
| 1-4 | 완료 | 2026-09-20. plants.last_watered_unknown 추가(0001, SPEC 11.1 반영). DB 테스트는 src/db/testing/test-db.ts 로 실제 .sql 을 인메모리 node:sqlite 에 적용해서 돌린다(스키마·마이그레이션 어긋남, 업그레이드 경로, 외래 키·cascade, JSON 왕복). 스키마를 바꾸면 pnpm db:generate, 안 하면 테스트가 실패한다. 테스트는 tsconfig.test.json(Node 타입)으로 따로 타입체크. 시뮬레이터에서 0000→0001 업그레이드 확인. 결정: 미루기 최대 3회는 coefficients 에(2-3), 계절 경계 조정은 season_overrides 에 시작일로 저장(설정 화면) |
| 1-5 | 완료 | 2026-09-20. 토큰에 ink(글자·선)·surface(카드 면)·radius·spacing·motion 추가(SPEC 14.2 반영), 글자색은 soil.wet 이 아니라 ink. 서체는 assets/fonts 의 OFL 파일을 useFonts 로 로딩. src/ui: AppText·Card·Button·SoilGauge(band·pot, 상태별 질감, 600ms 번짐). 흙 상태는 engine 의 getSoilGaugeState. 컴포넌트는 개발용 /dev/gallery 에서 확인(Expo Go: exp://127.0.0.1:8081/--/dev/gallery). 토큰 대비는 tokens.test.ts 가 지킨다. 테스트 174개 |
| 2-1 | 완료 | 2026-09-20. 공간 등록: 사진(앨범·카메라, 1280px JPEG 80%, EXIF 제거, 문서 폴더 기준 상대 경로 저장) → 방향 → 유형 → 빛 등급(engine defaultLightGrade, 수정하면 manual) → 이름(자동 제안, 중복이면 번호)·저장. 규칙은 src/spaces/registration.ts 순수 함수, 초안은 settings.draft_space 에 저장해 이어서 한다. 저장소 함수는 Database 타입(src/db/types.ts)을 받고 늘 await. 홈은 공간 탭(4-5) 전까지 등록한 공간 목록을 보여 준다. 미구현: 나침반 방위 자동 제안(실기기 필요). 테스트 231개 |
| 2-2 | 완료 | 2026-09-20. 식물 등록: 사진(최대 3장, 첫 장 대표) → 종(번들 시드 30종 src/species/seed.ts 텍스트 검색, 모르겠어요면 식물군 직접 선택) → 화분(기본 중) → 흙 → 공간 → 분재(분재 수종이면 자동 선택) → 별명·마지막 물 준 날·첫 물주기와 계산식. 규칙은 src/plants/registration.ts, 계산식 글은 src/plants/formula.ts(1.0 인 공간·흙·보정은 생략). 식물군은 분재 토글이 우선, 일반 종을 분재로 키우면 종별 주기 대신 분재군 기본값. 물 준 날은 기기 로컬 정오, next_water_at 은 그날 0시로 저장. 등록 화면 공통 틀은 ui/RegistrationShell, 사진은 photos/photo-store.ts, 계수는 src/coefficients.ts 의 currentCoefficients(3-1 에서 서버 값 연결). 홈은 식물 카드(D-day, 흙 게이지)와 공간 목록. 미구현: 사진 부위 태그(3-5), 손바닥 비교 일러스트(7-4). 테스트 295개 |
| 2-3 | 완료 | 2026-09-20. 하단 탭 4개(오늘·공간·식물·기록, app/(tabs))와 + 버튼. 오늘 탭: 날짜·계절 배지(누르면 계절 설명 시트), 밀림(빨간 점, 카드 탭·물 줬어요만, 내일로 없음)·오늘(스와이프와 버튼으로 물 줬어요·내일로)·다가옴(3일 내)·오늘 물 줬어요(moss 완료). 규칙은 src/plants/today.ts 순수 함수(classifyToday·planWatering·planPostpone), 저장은 db/watering.ts recordWatering. 물 줬어요 시트: 흙 3지선다(안 고르면 skipped)·잎 처짐 → U 갱신 → 오늘부터 다시 계산, 기록에는 갱신 뒤 주기·계수 스냅샷을 남긴다. 수경·수동 고정은 배우지 않고, 수경은 물이 탁했어요만 받아 events note 로 저장. 미루기 최대 횟수는 coefficients.maxPostpones(3, SPEC 5.5 반영), engine canPostpone. 시트는 expo-router formSheet(fitToContents, 안에 flex:1 금지), 방금 물 준 카드의 번짐은 zustand ui-store 로 한 번만. 공간·식물 탭은 목록만, 기록 탭은 최근 물주기 50건(타임라인·통계는 5-4). 미구현: 분재 흙 확인 카드(4-2), 경고 카드·날씨 아이콘(5주차), 다가옴 카드 탭 → 식물 상세(2-5). 테스트 325개 |
| 2-4 | 완료 | 2026-09-21. 로컬 알림(expo-notifications, Expo Go 로 확인). 순수 함수: plants/schedule.ts(다음 물주기 재계산: next_water_at 은 마지막 물 준 날 + 주기(모름이면 I/2) + 미룬 횟수의 저장값이라 계절·계수·공간이 바뀌면 다시 센다. 밀린 식물·미룬 식물은 그대로, 지난 날짜는 오늘로), notifications/settings.ts(notify_time·dnd_start·dnd_end 는 "HH:MM", 기본 08:00, 방해금지는 종료 시각으로 이동), plan.ts(14일치, 물주기·밀림(+1일, 이후 3일마다)·계절 전환. 같은 아침 알림은 하나로 합쳐 하루 1개, id 는 type-yyyymmdd-slot, 수경은 "물 갈 때"), forecast.ts(14일 안의 계절 전환을 내다보고 전환 뒤 예정 식물을 새 계절로 미리 센다). scheduler.ts rescheduleAll 은 Notifier 를 받아 가짜로 테스트하고, 전체 취소 대신 계획에 없는 id 만 지우고 덮어쓴다(iOS 전체 취소가 비동기라 첫 예약이 빠지는 것을 시뮬레이터에서 확인, SPEC 12.3 반영). 겹친 요청은 coalesce 로 하나로. 트리거: 앱 진입(AppState)·물 줬어요·내일로·식물 등록. 권한은 식물이 생긴 뒤 오늘 탭에서 한 번 묻고(온보딩 3.1 이 생기면 거기로 옮긴다), 거부하면 오늘 탭 상단 배너 + 설정 열기. 알림을 누르면 오늘 탭. 점검 화면은 /dev/notifications. 미구현: 백그라운드 fetch(개발 빌드·실기기 필요), 분재 흙 체크(4-2, 지금은 분재도 물주기 알림), 날씨·작업·분갈이·재확인 알림, 설정 화면의 알림 시각·방해금지 입력, 수동 고정 식물의 계절 전환 질문(2-5), 안드로이드 미확인. 테스트 387개 |
| 2-5 | 완료 | 2026-09-21. 식물 상세 app/plant/[id].tsx: 사용자 결정으로 계산식·계수는 화면에 내지 않고 결과만 말한다("7일마다 물을 줘요", SPEC 1·3.4·4.2 반영). plants/formula.ts 는 format.ts 로 바꾸고 formatFormula 를 없앴다(등록 마지막 화면도 결과만). 규칙은 plants/care.ts 순수 함수: planManualInterval(1~60일, null 이면 자동으로 되돌림, U 유지)·planRepot(U 1.0, last_repot_at, repot 이벤트)·planMove(U 유지, move 이벤트)·planRename·feedbackStreak(같은 흙 상태 3회 연속, skipped 는 건너뛰고 분갈이 전 기록은 제외)·needsSeasonQuestion. 직접 고친 것은 밀렸거나 미룬 식물도 바로 다시 센다(schedule.ts countWaterDate, 지난 날짜는 오늘로). plants.manual_season 추가(0002, SPEC 11.1): 직접 정한 뒤 계절이 바뀌면 상세에서 자동으로 바꿀지 한 번 묻는다. 편집은 app/sheet/plant-edit.tsx(field: name·interval·repot·move). 공간 이동만 목록이 길 수 있어 modal 로 띄운다: formSheet fitToContents 안에 ScrollView 를 두면 제목이 밀려난다. db: deletePlant(사진 경로를 돌려주고 파일은 호출한 쪽이 지움)·listPlantWaterings. 진입: 식물 탭 카드, 오늘 탭의 다가옴·완료 카드. 앱 문구를 전체적으로 다듬었다: 계산·기본값·등급·수종군 같은 말을 빼고 자연스러운 문장으로. 미구현: 관리 카드(3-4), 분재 작업 캘린더(4-3), 식물 탭 정렬·필터, 사진 추가·대표 사진 변경. 테스트 403개 |
| 디자인 | 완료 | 2026-09-21. 사용자가 Claude 디자인 캔버스의 세 시안 중 B 화원 라벨을 골라 확정(https://claude.ai/artifact/5F7GgdGMwxGj4X4nNKWoHy). 베이지·세리프·갈색 흙 게이지를 버리고 옅은 잎빛 바탕, 깊은 잎 초록(accent), 새순 연두 강조 면(highlight), 열매 자주(berry)로 바꿨다(SPEC 14장 전면 개정, 색 규칙도 변경). 토큰은 ink·sub·paper·surface·hair·soft·block·accent·onAccent·highlight·gaugeEmpty·berry·berryTint·berryEmpty·sprig 이고 대비는 tokens.test.ts 가 라이트·다크 모두 지킨다. 서체는 Pretendard 유지, 큰 숫자와 학명만 Figtree(@expo-google-fonts/figtree, 한글이 없어 한글이 섞이는 곳에는 쓰지 않는다). Instrument Serif 는 뺐다. SoilGauge 대신 DayGauge(하루 한 칸, 14칸 상한, gauge-cells.ts 순수 함수, engine getSoilGaugeState 에 totalDays 추가). 새 컴포넌트 Tag·Sprig, DaysLeft 는 DueTag(오늘·D-3·N일 지남·완료)로. Button 은 테두리 없는 primary·secondary·surface, Card 는 tone highlight, 글자 변형 formula 는 caption 으로 이름을 바꿨고 caption·scientific 은 기본이 보조 글자색. 게임처럼 보이지 않게 굵은 테두리·어긋난 그림자·아주 두꺼운 글자·왼쪽 색 선 상자는 쓰지 않는다. 테스트 400개 |
| 3-1 | 완료 | 2026-09-21. Supabase 프로젝트 없이 할 수 있는 부분까지. supabase/migrations/20260921000000_init.sql: species·coefficients·season_bounds·species_reports·weather_cache·usage_counters, 전 테이블 RLS(anon 은 종·계수·계절 경계 읽기와 신고 넣기만, 나머지는 service role). 사용자 데이터 테이블은 없다. 앱: src/coefficients/(parse.ts 서버 행 snake_case ↔ 엔진 camelCase, 전부 검사해서 하나라도 이상하면 버림 / index.ts currentCoefficients·loadCachedCoefficients·refreshCoefficients, settings.coefficients_cache / remote.ts EXPO_PUBLIC_SUPABASE_URL·EXPO_PUBLIC_SUPABASE_KEY 가 비면 호출 안 함, 키는 apikey 헤더로만(publishable 키는 JWT 가 아니라 Authorization 으로 인증 안 됨) / use-coefficients.ts 시작 시 캐시를 읽은 뒤 화면을 띄우고 서버 값은 뒤에서 받아 바뀌면 rescheduleSoon). seed.ts 가 번들 기본값에서 supabase/seed/coefficients.sql 을 만들고 seed.test.ts 가 어긋남과 RLS 누락을 지킨다(pnpm seed:coefficients). 2026-09-21 연결 완료: 프로젝트 jddopglqjtrvwmzylrlk, 마이그레이션·시드 적용, apps/mobile/.env(gitignore)에 URL 과 publishable 키. 실서버에서 확인함: 계수 14행 읽기 200, species·season_bounds 읽기 허용, weather_cache·usage_counters·species_reports 는 읽으면 행이 안 보임, coefficients·species 쓰기 401, 신고 넣기 201. 앱이 받아 settings.coefficients_cache 에 저장하는 것까지 시뮬레이터에서 확인. 남은 일: 서버에서 값을 바꿨을 때 앱에 반영되는지 한 번 보기(선택), species 300종은 3-4. 테스트 434개 |
| 3-2 | 완료 | 2026-09-21. supabase/functions/identify: PlantNet 연결. 순수 로직은 _shared/plantnet.ts(응답에서 후보 3개, 점수 0.01 미만은 버림), 한도는 _shared/limits.ts 와 bump_usage 함수(마이그레이션 20260921010000, 동시 요청에도 정확히 세고 48시간 지난 기록은 지움). 한도값은 coefficients 에서 읽는다. 사진은 넘기는 동안 메모리에만 둔다. 테스트 14개. 배포: pnpm functions:deploy 또는 npx supabase functions deploy <이름> --project-ref jddopglqjtrvwmzylrlk. import map 은 함수 폴더마다 둔 deno.json 이라야 번들러가 읽는다(공유 위치의 것은 무시된다) |
| 3-3 | 완료 | 2026-09-21. supabase/functions/species: 종 DB 에 없으면 Claude 로 만들어 검증 후 저장. _shared/species-schema.ts 는 zod 스키마와 상식 체크(다육인데 주기가 짧음, 열대인데 실내 월동 불가, 수종군·식물군 어긋남 등), 거부되면 이유를 넣어 한 번만 재생성하고 두 번 다 실패하면 pending. 모델은 claude-sonnet-5(SPEC 9.2 의 sonnet-4-6 보다 뒤 세대). 테스트 17개 |
| 3-4 | 완료 | 2026-09-21. scripts/species-list.ts 300종(SPEC 10.5 구성대로, 번들 30종과 철자 일치, 학명·국명 중복 없음). scripts/seed-species.ts 는 배포된 species 함수를 학명마다 부른다. 프롬프트와 검증이 갈라지지 않고, 이미 있는 종은 건너뛰어 끊겨도 다시 돌리면 된다. pnpm seed:species [--limit N] [--group X]. 2026-09-21 실행 완료: 서버에 300종. 모델 출력이 그때그때 달라 2종이 보류됐다가 재시도로 들어갔다. 주의: 프롬프트의 "분재로 키우는 수종"이 너무 넓어 율마·인삼고무나무 같은 일반 화분 식물이 분재로 분류됐다. 기준을 좁혀 다시 배포했지만 이미 생성된 행은 그대로다(SPEC 10.4 주 1회 검수 때 고친다) |
| 3-5 | 완료 | 2026-09-21. 고른 종은 출처와 무관하게 KnownSpecies 로 같게 다룬다(registration.ts). plants.base_interval 추가(0003)로 엔진이 시드 목록에 기대지 않고 오프라인에서도 같은 값으로 센다. api/client.ts(설정 없으면 호출 안 함)·identify.ts·species.ts(시드 → 기기 캐시 → 서버). 사진 단계에서 넘어갈 때 인식을 시작하고, 한도 초과·장애·못 찾음은 모두 텍스트 검색으로 넘어간다(시뮬레이터에서 폴백 확인). 식물 상세에 관리 카드. React Native 의 FormData 는 {uri,name,type} 객체를 받지 않는다: expo-file-system 의 File 이 Blob 이라 그대로 넣는다(photos/photoFile). 시뮬레이터에서 사진 → 인식(금귤 56%) → 종 생성·저장 → 등록 → 관리 카드까지 확인. 미구현: 부위 태그(사진은 leaf 로 보냄), Claude 비전 폴백(SPEC 9.1) |
| 4-2 | 완료 | 2026-09-21. 분재 알림 (SPEC 6.1): 물주기와 별개 알림으로 "흙 확인할 때". 폭염에는 아침·저녁 두 번, 겨울에는 11시(화분 속 물이 얼지 않게). 시각은 settings 의 bonsai_evening_time·bonsai_winter_time. 물 줬어요 시트는 분재면 "말랐어요 / 아직 촉촉해요" 두 가지로, 촉촉 응답 자체가 U 를 ×1.15 하고 내일로 미룬다(말랐음은 U 유지). 시뮬레이터에서 확인 |
| 4-3 | 완료 | 2026-09-21. 작업 캘린더 (SPEC 6.2): 등록할 때 종 DB 의 bonsai_tasks 를 plant_tasks 로 복사(plants/tasks.ts taskRowsFor, 서버 값이라 모양을 확인). 이번 달 할 일과 다음 작업을 식물 상세에 보여 주고 "했어요"로 완료(해마다 리셋). 해를 넘기는 작업(철사걸이 10~2월)도 센다. 알림은 시작 월 1일 아침 한 번. 시뮬레이터에서 확인 |
| 4-4 | 완료(일부) | 2026-09-21. 월동 경고 (SPEC 6.3): 겨울에 분재가 실내(창가·창에서 먼 실내)면 오늘 탭에 경고. 잡목·화목은 휴면을 못 함, 침엽은 실외 월동. 발코니 확장·테라스는 경고하지 않는다. 미구현: 한파·서리·발코니 경고는 날씨가 필요해 5주차 |
| 4-1 | 완료 | 2026-09-22. Edge `light-grade`(Claude 비전, 사진 1장 + 방향 + 유형 → 등급·확신도·근거·창/커튼/거리·한 줄 설명). 규칙은 supabase/functions/_shared/light.ts 순수 함수, askClaude 가 images 를 받는다. 확신도 0.5 미만이면 engine resolveLightGrade 가 방향 × 유형 기본값으로 가고(사람 > AI > 기본값), 읽은 값은 확신이 낮아도 spaces.ai_evidence 에 남겨 공간 상세에서 참고로 보여 준다. 모델이 "…것이 좋다"처럼 답할 때가 있어 한 줄 설명은 isPolite 로 걸러 버린다. 한도는 coefficients.daily_cap_light_grade(300), 넘거나 실패하면 앱은 조용히 기본값 표로 간다. 빛 단계에 들어올 때 한 번 부르고 사진·방향·유형이 바뀌면 다시 읽는다. 시뮬레이터에서 확인(강광·ai, 확신 낮은 사진은 기본값) |
| 4-5 | 완료 | 2026-09-22. 공간 탭 카드를 누르면 공간 상세 app/space/[id].tsx: 큰 사진·사진 다시 찍기(카메라·앨범, 새 사진으로 빛을 다시 읽음), 이 자리의 빛(밝기·어떻게 가늠했는지·사진에서 본 것 두 줄·한 줄 설명은 사진으로 정했을 때만), 밝기 바꾸기·이름 바꾸기는 sheet/space-edit.tsx, 계절 메모(spaces/season-memo.ts, 유형 × 계절 고정 문구. 겨울 실내 온풍 7.3, 장마 바깥 자리 7.4, 발코니 겨울 새벽 5도 3.3, 할 말 없는 계절은 비움), 이 공간의 식물 두 줄 그리드(누르면 식물 상세), 빈 공간만 지우기(식물이 있으면 옮기라고 안내, 명세에 없던 것을 더함). 규칙은 spaces/space-edit.ts 순수 함수: planLightGrade(가만히 둔 값을 고르면 manual 을 풀고 ai·default 로 돌아감)·planRetake(직접 고친 밝기는 사진을 바꿔도 남김)·planSpaceName. 빛이 바뀌면 spaces/save-space.ts 가 그 공간 식물의 다음 물주기를 다시 세고(plants/schedule planSpaceReschedule, 밀리거나 미룬 식물은 그대로) 알림을 다시 짠다. db: getSpace·updateSpace·deleteSpace(식물이 있으면 안 지움, 사진 경로를 돌려줌). BackButton 을 ui 로 옮김, 을/를은 lib/josa withObject. 시뮬레이터에서 확인: 중광 → 강광이면 몬스테라 9/28 → 9/27, 금귤 9/26 → 9/25, 미룬 곰솔은 그대로, 재촬영 뒤 옛 사진 파일 삭제, 빈 공간 삭제. 테스트 526개 |
| 5-1 | 완료 | 2026-09-22. Edge `weather`(GET ?region=지역 id): 기상청 단기예보(getVilageFcst) + 에어코리아 미세먼지 예보(getMinuDustFrcstDspth)를 하루 요약으로 준다. 순수 로직은 _shared/kma-grid.ts(활용가이드 LCC 변환, 서울 예시 60,127 과 같음)·regions.ts(시·도 17, 시·군·구 229. 특별·광역시는 구·군, 도는 시·군까지. id 는 "서울 강남구" 모양이고 좌표는 시청·군청 근처, 주요 21개 도시가 기상청 격자와 한 칸 안)·weather.ts(가장 최근 발표 시각, 인증키 이중 인코딩 방지, XML 오류 응답 읽기, 강수량 글자 → mm(범위는 아래쪽), 하루 요약 tmin·tmax·pop·pcp·windMax·condition, 권역별 미세먼지 등급은 PM10·PM2.5 중 나쁜 쪽). 캐시는 weather_cache 에 예보는 격자마다(kma:nx,ny), 미세먼지는 권역마다(air:권역) 한 행, coefficients.weather_cache_hours(3). 못 받으면 지난 값을 준다. 인증키는 Edge Function Secret DATA_GO_KR_SERVICE_KEY(공공데이터포털, 기상청·에어코리아 같은 키). 키가 없으면 503 not_configured 이고 앱은 날씨만 끈다. 2026-09-22 사용자가 Secret 을 넣었고 실서버에서 강남구 예보·미세먼지 받는 것 확인(DATA_GO_KR_SERVICE_KEY 하나로 두 서비스 모두 됨, 함께 넣은 DATA_GO_KR_SERVICE_KEY_weather 는 쓰지 않음). 밤 11시 넘으면 최신 발표에 오늘 칸이 없어 설정 화면은 내일 기온을 보여 준다(temperatureSummary) |
| 5-2 | 완료 | 2026-09-22. 설정 화면 app/settings.tsx(오늘 탭 오른쪽 위 조절 막대 아이콘으로 진입): 알림 시각·방해금지(sheet/time.tsx, iOS 는 돌림판, 안드로이드는 시계 창, 방해금지 끄기), 분재 더운 날 저녁·겨울 확인 시각, 날씨 지역, 버전. 지역은 sheet/region.tsx(시·도 → 시·군·구, 이름 찾기, 고르지 않기) 이고 settings.region_code 에 "서울 강남구" 모양 id 를 저장한다. 앱 목록 src/weather/regions.ts 는 서버 목록과 테스트로 맞춘다. 날씨는 src/weather: forecast.ts(서버 응답·저장값 검사, 날짜로 찾기, 48시간 넘거나 기기가 한국 시간이 아니면 끔, 3시간마다 다시 묻기)·refresh.ts(settings.weather_cache 에 저장, 못 받으면 같은 지역의 지난 값, 지역을 바꾸면 전 지역 값은 버림)·sync.ts(겹친 요청은 하나로, 새 예보면 알림 다시 짬)·store.ts(zustand). 앱 진입과 앞으로 올 때 받고, 백그라운드는 expo-background-task(최소 6시간, 시각은 기기가 정함, 날씨 뒤에 알림도 다시 짬). Expo Go iOS 에서는 백그라운드가 돌지 않아 개발 빌드에서 확인해야 한다. 시뮬레이터에서 지역 고르기·찾기·방해금지 저장과 끄기, 키가 없을 때 "지금은 날씨를 받지 못하고 있어요" 확인. 테스트 546개 |
| 5-3 | 완료 | 2026-09-22. 날씨 규칙 src/weather/rules.ts 순수 함수(SPEC 7.2, 6.3, 7.3, 7.4): rainWatered(오늘 강수확률 70% 이상 + 5mm 이상이면 테라스에서 오늘까지 물 줄 식물, 밀린 식물 포함, 수경 제외)·planRainWatering(배우지 않고 source=rain 기록)·weatherCards(폭염 33도: 테라스·발코니 확장 / 한파 -5도: 오전 9시 전이면 오늘 새벽, 아니면 내일 새벽, 분재·발코니 문구 / 서리 3도: 10~11월·3~4월 테라스, 한파면 생략 / 강풍 10m/s: 테라스, 작은 화분 따로 / 미세먼지 나쁨 이상: 열대 관엽이 있을 때 / 겨울 월요일 실내 관엽 습도 / 장마 시작일)·heatDays·weatherAlert. 적용은 rescheduleAll 안에서: 비는 기록 id 를 rain-식물-날짜로 정해 하루 한 번만, 폭염인 날 바깥 식물이 든 아침 알림(물주기·밀림·분재 아침)은 07시로 당기고, 내일 새벽 한파·서리면 "한파 예보" 알림을 받은 직후(06시 이후, 방해금지 피함)에 한 번 울린다(settings.weather_alert 로 같은 새벽 중복 방지). 날씨는 고른 지역 것이고 48시간 안이며 한국 시간일 때만 쓴다(loadUsableWeather). 오늘 탭: 계절 이름표 옆 날씨 아이콘과 최고기온, 경고 카드(분재 월동 6.3 도 같은 카드로 합침, 경고는 자줏빛·안내는 연두, 닫으면 그날은 안 뜸 settings.dismissed_cards). 설정 화면에 오늘 기온·강수확률 한 줄. 기록 탭에 "비가 대신 줬어요". 시뮬레이터에서 날씨를 넣어 아이콘·미세먼지 카드·닫기 확인 후 되돌림. 테스트 583개 |
| 5-4 | 완료 | 2026-09-22. 기록 탭(SPEC 3.5): 날짜별(식물 고르기 이름표, 날짜 제목 아래 카드에 식물·한 줄)과 식물별 통계(평균 물 준 간격, 지금 알림 간격, 최근 30일 밀림 횟수, 누르면 식물 상세). 규칙은 src/records/timeline.ts(buildTimeline 물주기·이벤트 합쳐 날짜별, entryText 물 줌·흙·잎 처짐·비가 대신 줬어요·비료·분갈이 화분과 흙·상태 진단·작업 마침·공간 이동(로/으로)·물이 탁했어요)와 stats.ts(plantStats: 같은 날 두 번은 한 번, 최근 간격 10개 평균, 밀림은 due_at 보다 늦게 준 물 + 지금 밀림). watering_logs.due_at 추가(0004, SPEC 11.1 반영): 물 주기 전 예정일이라 "내일로" 미룬 것은 밀림으로 안 센다. 0004 전 기록은 null 이라 밀림에서 빠진다. 분재 작업 "했어요"는 events 에 task 이벤트로 남기고(id task-작업-연도) 되돌리면 지운다. db: listRecentEvents. ui: Chip. lib/josa withTo. 주의: Metro 가 돌 때 app/ 폴더에서 sed -i 를 쓰면 임시 파일을 라우트로 읽다 죽는다(파이썬이나 Edit 로 고친다). 시뮬레이터에서 두 보기 확인. 테스트 599개 |
| 5-5 | 완료 | 2026-09-22. 비료·분갈이 (SPEC 8.2, 8.3): 규칙은 src/plants/feeding.ts 순수 함수. fertilizerRule(종 DB fertilizer 가 먼저, 없거나 틀리면 식물군 기본값: 관엽 4~10월 4주, 다육 4~6·9~10월 8주, 허브 3~10월 2주, 분재 4~6·9~10월 4주)·isFertilizerDue(장마·폭염·겨울 쉼, 분갈이 뒤 4주 쉼, 기록이 없으면 등록일부터 셈. SPEC 8.2 에 적음)·repotHint(권장 주기 다육 24·관엽 18·허브 12개월, 종 DB repot_months·repot_season 이 먼저, 적기 기본 3~4월, 모르면 등록일부터, "바싹 말랐음" 최근 5번 중 4번이면 뿌리 확인, 분재·수경 제외). 알림: 물 줄 날 비료 차례면 "몬스테라 물 줄 때, 비료도 함께"(일부만이면 따로 한 줄, 분재 아침 확인에도 한 줄), 분갈이는 적기 월 1일 아침에 분재 작업과 한 알림(작업 없으면 제목 "분갈이 검토", "에케베리아 마지막 분갈이 2년 지났어요"). 스케줄러가 종 캐시와 마지막 fertilize 이벤트를 읽는다(db/events latestEventAt). 물 줬어요 시트: 비료 차례면 "비료도 줬어요"(분재는 말랐어요 위), 고르면 fertilize 이벤트. 오늘 탭: 분갈이 검토·뿌리 확인 카드(plants/feeding-db.ts repotHints). 시뮬레이터에서 비료 선택지 확인 후 되돌림. 테스트 624개 |
| 6-1 | 완료 | 2026-09-22. 상태 진단 (SPEC 8.1, 9.3, 11.3): Edge `diagnose`(POST multipart: 사진 1~3장, device_id, species, group_code, 최근 물주기 5건). 규칙은 _shared/diagnose.ts(프롬프트, zod 검증, 진단명 확신 높은 것부터 2개·할 일 3개, 원인·할 일이 존댓말이 아니면 이유를 넣어 한 번 더 만들고 두 번째는 받아 줌, 숫자 범위 붙임표와 긴 대시는 물결표·쉼표로 tidy, 기기 id 는 SHA-256 해시로만 셈). 한도는 기기당 하루 coefficients.diagnose_daily_limit(3)와 전체 daily_cap_diagnose(200), 넘으면 429 limit. 공유 모듈끼리 값 import 가 되게 supabase/tsconfig 에 allowImportingTsExtensions. 앱: lib/device-id.ts(첫 사용 때 uuid, settings.device_id), api/diagnose.ts, diagnose/diagnosis.ts(응답·저장값 검사, 확신도는 "가능성 높음·있음·낮음"으로). 화면: 식물 상세 "상태 진단" → app/diagnose/[id].tsx(모달, 사진 1~3장, 진단하기, 오늘 다 썼으면 "내일 다시") → app/diagnosis/[eventId].tsx(심각하면 위에 "빨리 손써 주세요", 의심되는 것, 까닭, 지금 할 일, N일 뒤 다시, 남은 횟수, "AI 추정이며 정확하지 않을 수 있어요"). 결과는 events 에 diagnose 로 남기고 첫 사진만 썸네일로 둔다(나머지 사진은 지움, 그냥 나가면 고른 사진 모두 지움). 기록 탭에 "상태 진단, 과습"처럼 보이고 누르면 결과를 다시 연다. 식물을 지우면 진단 사진도 지운다. 실서버와 시뮬레이터에서 금귤 사진으로 확인. 재확인 알림·물주기 반영은 6-2 |
| 6-2 | 완료 | 2026-09-22. 진단 재확인과 물주기 반영 (SPEC 8.1, 12.1): 결과 화면에 "N일 뒤에 알려 주세요"(진단한 날 + recheck_days, 받으면 날짜와 "알림 끄기"), 알림은 그날 알림 시각에 "다시 확인해 주세요 / 금귤 진단한 지 7일 지났어요", 하나면 누르면 그 진단 결과로 가고(target diagnosis + eventId, use-notifications 가 연다) 여럿이면 묶어서 오늘 탭. 재확인 날짜와 답은 진단 이벤트 payload(recheckDate, hintAnswer)에 남기고 스케줄러가 listDiagnoses 로 읽는다(db/events updateEventPayload). 물주기 반영: watering_hint 가 over·under 이고 자동으로 세는 식물(직접 정함·수경 아님)이면 "간격 늘리기 / 더 자주 알리기 / 그대로 두기"를 한 번 묻고, 반영하면 U ×1.15·×0.85(plants/care.ts planWateringHint, 흙 상태 답과 같은 계수) 후 바로 다시 센다. 시뮬레이터에서 재확인 설정·물주기 질문·그대로 두기 확인 후 시험 기록은 지움 |
| 6-3 | 완료(일부) | 2026-09-23. 사진 (SPEC 8.4): 물 줬어요 시트에 "오늘 사진도 남기기"(사진 찍기·앨범, 빼기, 분재는 두 갈래 답이라 빼둠), 저장하면 photos 행으로 남고 식물당 최근 100장만 유지한다(db/photos.ts addPlantPhoto 가 넘치는 경로를 돌려주면 파일도 지움). 식물 상세는 사진이 있으면 대표 사진 대신 슬라이더(plants/photo-slider.tsx: 가로로 넘기고 찍은 날과 "1 / 2"를 얹음, 최근 것부터). 미구현: 영상으로 내보내기(1초 1장 mp4)는 기기에서 영상을 만드는 네이티브 모듈이 있어야 해서 남김. 시뮬레이터에서 사진 남기기와 넘김 확인 |
| 6-4 | 완료 | 2026-09-23. 내보내기·가져오기·전체 삭제 (SPEC 3.6·8.4·9장 데이터 이전). 순수 함수는 src/data/backup.ts(파일 모양 {app:'multtae', version:1, exportedAt, appVersion, 표 7개}, parseBackup 은 우리 앱·판만 받고 외래 키가 끊긴 행과 모르는 설정 키는 버린다), DB 는 store.ts(readAll·deleteAll·replaceAll, 사진 경로를 돌려주고 파일은 부르는 쪽이 지운다), 파일·압축은 archive.ts(jszip + expo-sharing + expo-document-picker, 캐시의 exports 폴더에 multtae-YYYYMMDD.zip 을 만들어 공유 시트로 건넨다. 사진은 문서 폴더 기준 상대 경로 그대로 넣고, 가져올 때 .. 나 / 로 시작하는 경로는 버린다. JSON 만 있는 파일도 읽는다). 서버로는 아무것도 보내지 않는다. 설정 화면에 데이터 칸 3줄, 가져오기·전체 삭제는 Alert 로 한 번 묻고 끝나면 결과를 아래 한 줄로 알린다. 바뀐 뒤에는 설정을 다시 읽고 화면·알림·날씨를 다시 맞춘다. 시뮬레이터에서 내보내기(2MB, json + 사진 6장) → 전체 삭제(표·사진 파일 모두 빔) → 가져오기(행·사진 그대로 복원) → 남의 json 고르기(안내만 내고 기존 데이터 유지)까지 확인. 테스트 652개 |
| 6-5 | 완료 | 2026-09-23. 다크 모드 마감과 접근성 라벨 (SPEC 14.4·15). 다크로 전 화면을 훑어 강조 면 위 게이지의 빈 칸이 카드 면보다 어두워 구멍처럼 보이던 것을 고쳤다: 토큰 gaugeEmptyOnHighlight(라이트는 흰색 그대로, 다크 #465B4E)를 더하고 tokens.test.ts 가 "빈 칸은 카드 면보다 밝다, 찬 칸과는 3 이상"을 라이트·다크 모두 지킨다. 접근성: 누를 수 있는 카드는 스크린리더가 안의 글자 대신 라벨만 읽으므로 라벨에 요약을 담았다(plants/format.ts speakDaysLeft 는 D-3 을 "3일 뒤"로 읽고, plantCardLabel 은 "곰솔, 남향 실내 창가, 1일 지남", spaces/format.ts formatPlace·formatSpaceLine·spaceCardLabel, 기록 탭 통계 카드도 같게). 큰 글씨(accessibility-extra-large)에서 오늘 탭이 무너지지 않는 것과, 다크에서 오늘·공간·공간 상세·식물 상세·기록·설정·등록·시트·갤러리를 시뮬레이터로 확인. i18n/hardcoded.test.ts 가 app·src 문자열에 한글이 남지 않았는지 본다(SPEC 15 다국어, 말 자체인 josa.ts·regions.ts·종 시드·ko.ts 만 예외). 테스트 663개 |
| 7-1 | 완료(일부) | 2026-09-23. 목록 성능 (SPEC 15 성능). 식물 203개를 넣고 재 보니 식물 탭 첫 표시가 2650ms 였다(카드를 한 번에 다 그렸다). 오늘·공간·식물·기록 탭과 공간 상세의 목록을 FlatList 로 바꿔 보이는 만큼만 그리게 했더니 169ms. 카드 한 줄은 plants/plant-row.tsx 로 빼서 memo 하고, 누를 때 쓰는 함수는 id 만 받아 화면마다 새로 만들지 않는다. 공간별 식물 수와 식물별 물 준 기록은 목록을 되풀이해 훑지 않고 한 번에 묶어 센다. 구역이 있는 화면(오늘 탭, 공간 상세)은 제목과 카드를 한 줄짜리 목록으로 펴고 위아래 덩어리는 ListHeader·ListFooter 로 둔다. 시뮬레이터에서 203개로 스크롤·스와이프·물 줬어요를 확인하고 시험 데이터는 지웠다. 남은 일: 저사양 안드로이드 실측과 콜드 스타트 3초는 실기기·릴리스 빌드가 있어야 한다 |
| 7-2 | 완료 | 2026-09-23. 경계 테스트와 자정 고침 (SPEC 15 시간). src/scenarios/boundaries.test.ts: 서울·로스앤젤레스·런던·오클랜드 네 시간대를 돌며 같은 순간을 오늘 탭·다음 물주기·알림이 어떻게 보는지 함께 본다(밤 11시 59분은 오늘, 자정을 넘기면 밀림, 밤늦게 준 물은 그날 완료, 알림은 기기 시간대 아침 8시). 계절은 기기 시간대와 무관하게 Asia/Seoul 날짜로 보고(로스앤젤레스에서 아직 11월 15일이어도 겨울), 물 준 시각을 정오로 남기는 까닭(오프셋이 한두 시간 달라져도 날짜가 안 흔들린다)도 테스트로 남겼다. 전체 테스트를 6개 시간대(UTC·LA·오클랜드·서울·런던·상파울루)에서 돌려 같은 결과를 확인. 고친 것: 오늘 탭을 열어 둔 채 자정을 넘기거나 앱을 밤새 뒤에 두었다 돌아오면 어제 날짜와 어제 구역이 남아 있었다. day-boundary.ts msUntilNextDay(순수)와 use-fresh-day.ts 로 자정 1초 뒤와 앱이 앞으로 올 때 useGarden·useRecords 가 다시 읽는다(시뮬레이터에서 뒤에 둔 사이 DB 를 바꿔 확인). 테스트 697개 |
| 7-3 | 완료 | 2026-09-23. 빈 상태·에러·오프라인 전수 점검 (SPEC 15 오프라인). 고칠 것이 없어 코드 변경은 없다. 빈 상태는 오늘(공간 없음·식물 없음·할 일 없음)·공간·식물·기록(전체·거른 것·식물별)·공간 상세·식물 상세·진단 결과·지역 검색까지 다 있고, 실패 문구는 사진 저장·저장 실패·다시 찍기·진단·내보내기·가져오기·전체 삭제·DB 준비에 있다. 오프라인은 EXPO_PUBLIC_SUPABASE_URL 을 닿지 않는 주소로 두고 Metro 를 다시 띄워 확인했다: 홈·목록·상세·기록·설정은 그대로 뜨고(계수는 캐시), 사진 인식은 이름으로 찾기로 넘어가 번들 30종을 그대로 찾고, 검색 결과가 없으면 모르겠어요 안내가 붙고, 진단은 지금은 진단할 수 없어요, 날씨는 지금은 날씨를 받지 못하고 있어요가 뜬다. 지역 목록은 번들이라 오프라인에서도 고를 수 있다. 확인 뒤 .env·지역 설정·초안·시험 사진을 모두 되돌렸다 |
| 7-4 | 완료(일부) | 2026-09-23. 온보딩과 앱 아이콘·스플래시 (SPEC 3.1, 14.4). 온보딩은 app/onboarding.tsx 두 장: 공간부터라는 한 줄과 잔가지 그림, 그다음 알림 권한(거부해도 그냥 넘어간다). 끝나면 공간 등록으로, 건너뛰면 탭으로 가고 settings.onboarding_done 이 남아 다시 뜨지 않는다(src/onboarding/use-onboarding.ts, 루트 레이아웃이 준비된 뒤 replace). 오늘 탭의 권한 묻기는 건너뛴 사람을 위해 그대로 뒀다. 아이콘은 scripts/icons.py 가 앱 안의 잔가지(src/ui/sprig.tsx)와 같은 선을 그려 만든다: 베지에를 잘게 잘라 둥근 붓을 찍고 네 배로 그린 뒤 줄여 이음매가 없다. 새순 연두 바탕에 깊은 잎 초록, iOS 1024·안드로이드 적응형(앞면·바탕·단색)·스플래시(밝은 쪽·어두운 쪽) 를 한 번에 만든다. ios.icon 이 Expo 기본 번들(파란 로고)을 가리키고 있어 우리 PNG 로 바꾸고 assets/expo.icon 은 지웠다. 시뮬레이터에서 첫 실행 온보딩 두 장과 다시 실행 확인. 미확인: 아이콘·스플래시는 Expo Go 에서 Expo 것이 보여 실제 모습은 개발 빌드에서 봐야 한다 |
| 7-5 | 완료(일부) | 2026-09-23. 앱 서명 헤더와 일일 상한 (SPEC 9, 13.3). 서명: 헤더 `x-app-signature: <타임스탬프>.<hex HMAC-SHA256>` 이고 서명 대상은 `<타임스탬프>.<함수 이름>` 이라 다른 함수의 서명을 못 쓴다. 앞뒤 5분까지 받는다. 서버는 _shared/signature.ts 가 확인하고 다섯 함수 모두 앞에서 막는다(401). APP_SIGNATURE_SECRET 이 비어 있으면 확인하지 않아, 시크릿을 넣기 전에도 그대로 돈다. 앱은 src/api/signature.ts 의 HMAC(기기에 crypto.subtle 이 없어 expo-crypto 의 SHA-256 으로 RFC 2104 을 만들었고, Node 의 createHmac 과 같은 값이 나오는지 테스트로 지킨다)을 client.ts 가 붙인다. expo-crypto 는 시크릿이 있을 때만 동적으로 불러온다(테스트에서 네이티브 모듈을 끌어오지 않게). seed-species 스크립트도 같은 방식으로 서명한다. 실서버에서 확인: 시크릿을 잠깐 켜고 서명 없이 401, 올바른 서명 200, 다른 함수 이름의 서명 401 을 보고 시크릿을 지웠다. 켜려면 서버 Secrets 의 APP_SIGNATURE_SECRET 과 앱의 EXPO_PUBLIC_APP_SIGNATURE_SECRET 에 같은 값을 넣으면 된다(한쪽만 넣으면 안 된다, .env.example 에 적어 뒀다). 상한: species 함수에 하루 상한이 없어 daily_cap_species(100) 를 더했다. 이미 있는 종은 캐시로 돌아가므로 새로 만들 때만 센다. 서버 coefficients 에 행을 넣는 것은 service role 이 필요해 다음 시드 적용 때 들어간다(그때까지는 함수 기본값 100 으로 같게 돈다). Sentry 는 네이티브 모듈이라 Expo Go 에서 안 돌아 자리만 만들었다: src/telemetry/report.ts reportError 로 날씨·알림의 삼킨 오류를 모으고, 실제 연결은 개발 빌드를 만드는 8-3 에서 한다. 테스트 701개(앱)·107개(서버) |
