# Supabase 프로젝트 주소와 anon 키 (SPEC 11.2, 17). 둘 다 공개되어도 되는 값이고 RLS 가 막는다.
# 비워 두면 앱은 번들 기본 계수로 돈다. PlantNet·Claude 같은 비밀 키는 여기에 넣지 않는다 (Edge Function 환경변수).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
